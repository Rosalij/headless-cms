// This is the PostCSS configuration file for the application.
// It defines the PostCSS plugins used for processing CSS in the project.
// In this case, it includes the Tailwind CSS plugin, which allows us to use Tailwind's utility classes in our stylesheets.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
